status: ok
next: merge
artifact: pr-metadata.json
`ap compile` now rejects Claude-specific profile fields when no compile target includes the `claude` harness.

expanded scope: agent-profile/agent_profile/compile_target_presence.py — required to wire harness field coverage into `ap compile` validation
