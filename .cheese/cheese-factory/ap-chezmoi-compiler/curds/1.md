status: ok
next: merge
artifact: pr-metadata.json
`ap install` now exits non-zero with the migration guidance required by curd #1.

expanded scope: agent-profile/agent_profile/cli.py — required to route `ap install` to the new command policy
