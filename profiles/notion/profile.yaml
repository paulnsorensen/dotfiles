# notion — Notion-connected session (ccp parity).
# notion was additive-only under ccp (mcp-add.json, no scope). Under the
# launch overlay it becomes a closed world whose single MCP is notion.
name: notion
description: Notion HTTP MCP
isolated: true
system_prompt: CLAUDE.md
# Auto-approve the profile's own MCP (ccp settings-merge parity).
permissions_allow:
  - "mcp__notion__*"
mcps:
  - name: notion
    type: http
    url: https://mcp.notion.com/mcp
