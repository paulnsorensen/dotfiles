# rtkonly — experimental: route file I/O through rtk + tilth only.
# Closed MCP world of just tilth.
name: rtkonly
description: Experimental — tilth + rtk only
isolated: true
system_prompt: CLAUDE.md
mcps:
  - name: tilth
    command: tilth
    args: ["--mcp", "--edit"]
