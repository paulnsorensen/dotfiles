# MCP Registry - Source of truth for Claude Code MCP servers
# Run `mcp-sync` to sync this to your Claude Code installation
#
# Scopes:
#   user    - Available in all projects (recommended for dev tools)
#   project - Stored in .mcp.json, shared with team
#   local   - Machine-specific, gitignored
#
# Gating:
#   gate_unless: VAR  - skip install when env var VAR == "true"
#                       (used to defer to a plugin's bundled MCP when active)

mcps:
  # context7, tavily — provided by the cheese-flow plugin's .mcp.json when
  # CHEESE_FLOW=true. Plugin MCPs are session-wide and accessible to all
  # skills/agents. Registering them here too would spawn duplicate processes.
  # tilth is registered below with gate_unless so we keep cheez-* working
  # whenever cheese-flow is dark.

  # shadcn lives in the FE profile only — see claude/profiles/fe/mcp-add.json.
  # Installing user-scope loads its tool schemas in every session even though
  # shadcn is only useful for FE work.

  tilth:
    command: tilth
    args: ["--mcp", "--edit"]
    scope: user
    description: AST-aware code search/read/edit (Tree-sitter); backs cheez-* skills when cheese-flow is unavailable
    gate_unless: CHEESE_FLOW

  context7:
    command: npx
    args: ["-y", "@upstash/context7-mcp"]
    env:
      CONTEXT7_API_KEY: "${CONTEXT7_API_KEY}"
    scope: user
    description: Library/framework docs (React, Tailwind, Next.js, …); backs /briesearch and /fetch when cheese-flow is unavailable
    gate_unless: CHEESE_FLOW

  tavily:
    command: npx
    args: ["-y", "tavily-mcp@latest"]
    env:
      TAVILY_API_KEY: "${TAVILY_API_KEY}"
    scope: user
    description: AI-powered technical web research; backs /briesearch when cheese-flow is unavailable
    gate_unless: CHEESE_FLOW

  serper:
    command: npx
    args: ["-y", "serper-search-scrape-mcp-server"]
    env:
      SERPER_API_KEY: "${SERPER_API_KEY}"
    scope: user
    description: Google SERP data via Serper.dev — web search, Knowledge Graph, People Also Ask, and page scraping

  todoist:
    command: npx
    args: ["-y", "@doist/todoist-ai"]
    env:
      TODOIST_API_KEY: "${TODOIST_API_KEY}"
    scope: user
    description: Official Todoist MCP (Doist-maintained) — invoke only when explicitly requested

  code-review-graph:
    command: uvx
    args: [code-review-graph, serve]
    scope: user
    description: Structural code knowledge graph via Tree-sitter — semantic search, call chains, impact radius, flows, communities
