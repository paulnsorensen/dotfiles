# Dot-paths exempt from the unknown-key halt gate in
# chezmoi/dot_claude/modify_settings.json (prefix semantics: the path and its
# whole subtree). Live values at these paths are overlaid back onto the
# merged settings.json output instead of being wiped. Blank lines and
# "#" comments are ignored.

tui
# env.SSL_CERT_FILE is intentionally NOT listed here — its gate exemption
# lives in the hard-coded $known entry in dot_claude/modify_settings.json.
