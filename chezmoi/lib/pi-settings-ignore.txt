# Dot-paths that Pi owns at runtime in ~/.pi/agent/settings.json. The guard
# chezmoi/dot_pi/private_agent/modify_settings.json keeps their live values
# without a warning (prefix semantics: the path and its whole subtree), unless
# the registry owns the same leaf. Blank lines and "#" comments are ignored.

# The last changelog version Pi showed.
lastChangelogVersion
