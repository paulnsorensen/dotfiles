{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://easy-cheese.dev/schemas/ultracook-pr-plan.schema.json",
  "title": "Ultracook PR Plan",
  "description": "Phase 7 PR-layout document written by /ultracook at .cheese/ultracook/<slug>/pr-plan.yaml. Consumed by the ultracook.pyz pr_plan_to_branches subcommand to emit branch / cherry-pick / PR-create commands.",
  "type": "object",
  "required": ["plate_layout", "shape", "groups"],
  "properties": {
    "plate_layout": {
      "type": "string",
      "enum": ["single", "stacked"],
      "description": "Copy of the authoritative manifest topology resolution; a plan cannot override an explicit or previously verified choice."
    },
    "shape": {
      "type": "string",
      "enum": ["single", "orthogonal_flat", "stacked_linear", "diamond_stack"],
      "description": "Layout shape chosen by the PR planner based on the dependency structure."
    },
    "groups": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["branch", "title", "base", "commits"],
        "properties": {
          "branch": {
            "type": "string",
            "description": "Branch name (kebab-case, slug + sequence + role)."
          },
          "title": {
            "type": "string",
            "description": "Conventional Commits-style PR title."
          },
          "body": {
            "type": "string",
            "description": "1-3 sentence PR body describing what the group ships."
          },
          "base": {
            "type": "string",
            "description": "Branch this PR targets. `main` for the root of a stack or any orthogonal-flat PR; otherwise the previous stack member's branch."
          },
          "commits": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "pattern": "^[0-9a-fA-F]{7,40}$"
            },
            "description": "Ordered list of commit SHAs from the manifest (7-40 hex chars; matches git's default short-SHA floor and rejects option-shaped strings like `--abort`)."
          },
          "depends_on": {
            "type": "array",
            "items": {"type": "string"},
            "description": "Branches that must be merged before this PR."
          },
          "pr_number": {
            "type": ["integer", "null"],
            "description": "Filled in by Phase 7 after `gh pr create`."
          },
          "pr_url": {
            "type": ["string", "null"],
            "description": "Filled in by Phase 7 after `gh pr create`."
          }
        }
      }
    }
  }
}
